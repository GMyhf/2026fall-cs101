# -*- coding: utf-8 -*-
"""第16周 课程知识体系总结、期末复习与上机考试"""

META = {
    'title': '第16周　课程知识体系总结、期末复习与上机考试',
    'subtitle': '知识体系 · 考试须知 · 覆盖矩阵 · 建议样卷 6 题 · 备选题库 · 备考清单',
    'footer': '计算概论（B） · 第16周 · 闫宏飞 · 2026 Fall',
    'info': ['北京大学　《计算概论（B）》',
             '主题与学习重点：知识图谱、神经网络等 AI 专题；课程知识体系总结与期末复习。'],
}

SLIDES = [
    ('section', '第 1 节', '课程知识体系总结'),

    ('ascii', '一张图看完 16 周', r"""
                    《计算概论（B）》知识体系
                              |
    +--------------+----------+----------+---------------+
    |              |                     |               |
 计算机基础     语言与工具          算法与数据结构      AI 素养
 (W1,W3,W13)   (W1,W2,W4)          (W6-W12,W16)      (W1,W14,W15)
    |              |                     |               |
 图灵机         Python 语法         复杂度分析        LLM 原理
 冯诺依曼       容器与代价          矩阵/前缀和       幻觉与验证
 二进制/补码    Shell & VM          排序/二分         提示词
 浮点 & ASCII   IDE & 调试          贪心/区间         知识图谱
 进程 & 虚拟内存 快速 IO            栈/队列           神经网络
 缓存 & 局部性                      递归/回溯         RAG
                                    并查集
                                    动态规划
                                    BFS / Dijkstra
"""),

    ('ascii', '算法主线：一条演化路径', r"""
   暴力枚举 --剪枝--> 回溯 (W9)
       |
       +--排序后一遍扫--> 贪心 (W6, W10)
       |                     |
       |                 贪心失效
       |                     v
       +--记忆化--------> 动态规划 (W10, W11)
       |
       +--按层扩展------> BFS (W12) --边权不同--> Dijkstra (W12)

   横向工具：前缀和/差分、二分答案、单调栈/单调队列、并查集
"""),

    ('table', '每周一句话（上）', [
        ['周', '一句话'],
        ['1', '三个平台各司其职；AI 是加速器不是替身'],
        ['2', 'python3 a.py < in.txt 是本课最常用的一条命令'],
        ['3', '补码让减法变加法；浮点永远不要用 == 比较'],
        ['4', '容器复杂度表要背；先看数据范围再定算法'],
        ['5', '月考是体检；订正 = 分类 + 关题解重写'],
        ['6', '贪心 = 排序 + 一遍扫，难在选排序键'],
        ['7', '栈用 list、队列用 deque；单调结构把 O(n^2) 降到 O(n)'],
        ['8', '递归三法则；递归就是在用系统栈'],
    ]),

    ('table', '每周一句话（下）', [
        ['周', '一句话'],
        ['9', '回溯 = DFS + 撤销；并查集摊还近 O(1)'],
        ['10', '要"多"按右端点排，要"合/盖"按左端点排；DP 三要素'],
        ['11', '0-1 倒序、完全正序；"恰好装满"要用 ±inf 初始化'],
        ['12', '用 deque、入队标记；要"最短"就 BFS'],
        ['13', 'Python 慢就换算法，不要抠常数'],
        ['14', '幻觉源于"训练目标是合理而非正确"'],
        ['15', '反向传播本质上就是动态规划'],
        ['16', '考场流程：看范围 → 认题型 → 定策略 → 造数据 → 查格式'],
    ]),

    ('section', '第 2 节', '期末上机考试须知'),

    ('table', '考试须知', [
        ['项目', '规定'],
        ['时间 / 时长', '第 16 周（日期节次以通知为准）；112 分钟'],
        ['题量 / 总分', '6 道编程题；100 分'],
        ['平台 / 语言', 'OpenJudge（cs101 小组）；Python 3 为主，允许 C++'],
        ['环境', '考试专用机房，断开外网'],
        ['允许携带', '一页 A4 双面手写 cheat sheet（不得打印、不得夹带整页代码）'],
        ['禁止', '⚠️ 任何 AI 工具（含本地模型、IDE 补全插件）、联网查询、任何交流'],
        ['学术诚信', '⚠️ 无法解释自己提交的代码，按学术不端处理，成绩记 0'],
    ]),

    ('bullets', '成绩参考（历年经验，以当学期通知为准）', [
        '**AC 5–6 题**：基本可获优秀',
        '**AC 4 题**：若笔试成绩较高且未超优秀比例限制，仍有机会评优',
        '**AC 0 题**：即使笔试满分，总评也会受到明显影响',
        '⚠️ 上述比例与条件**不是本讲义能确定的**，仅用于说明机考的重要性',
    ]),

    ('bullets', '考查的五项能力', [
        '**问题抽象**：把文字描述翻译成数据结构与状态模型 —— **最常见的失分点**',
        '**算法选择**：根据数据范围选出复杂度可行的算法',
        '**代码实现**：在**无 AI 辅助**下正确、快速地写出可运行代码',
        '**边界处理**：n = 0/1、多组数据、全相同、极值、不连通、空输入',
        '**调试能力**：读懂 WA / TLE 反馈并自行构造数据定位',
    ]),

    ('section', '第 3 节', '知识点覆盖矩阵与难度梯度'),

    ('table', '知识点覆盖矩阵（教师用）', [
        ['大类', '周次', '本卷覆盖', '具体考点'],
        ['语法与容器', 'W1–W2、W4', 'T1', '字典计数、多关键字排序、格式化输出'],
        ['计算机基础', 'W3、W13', '备选', '进制、位运算、ASCII、复杂度估算'],
        ['线性结构', 'W7', 'T2', '栈 / 队列 / 单调栈'],
        ['排序与贪心', 'W6、W10', 'T3', '排序键、区间问题、差分'],
        ['搜索', 'W9、W12', 'T4', 'BFS / DFS / 回溯 / 带状态搜索'],
        ['动态规划', 'W10–W11', 'T5', '线性 DP / 背包'],
        ['并查集与图', 'W9、W12', 'T6', '连通性、最小瓶颈路、二分答案'],
    ], '保证覆盖至少 5 个大类，且贪心、DP、搜索各至少 1 题'),

    ('ascii', '难度梯度', r"""
   T1   *          签到题，90% 学生应 AC        -- 保证及格线
   T2   **         基础数据结构应用，70% AC
   T3   ***        需要一次转化（排序键），55% AC
   T4   ***        带状态搜索，45% AC
   T5   ****       DP，状态设计有难度，30% AC
   T6   *****      综合建模，15% AC             -- 区分优秀
""", '⚠️ 预期 AC 率是命题经验估计，无历史数据支撑，需按实际结果逐年校准'),

    ('section', '第 4 节', '建议样卷（6 题 / 100 分）'),

    ('bullets', 'T1 成绩单核对（15 分）★☆☆☆☆', [
        '**考点**：字典计数、多关键字排序、格式化输出（W2、W4）',
        'n 条 `学号 科目 分数` 记录（n ≤ 2×10⁵）。按**总分降序**输出学号、总分、平均分（两位小数）；',
        '**总分相同时按学号字典序升序**。',
        '**卡点**：n=2×10⁵ 但只有 500 个学号（卡 `list.count()`）；平均分为整数时必须输出 `85.00`',
    ]),

    ('code', 'T1 参考解答', '''import sys
from collections import defaultdict


def solve():
    data = sys.stdin.read().split()
    n = int(data[0])
    total, cnt = defaultdict(int), defaultdict(int)
    idx = 1
    for _ in range(n):
        sid, _subject, score = data[idx], data[idx + 1], int(data[idx + 2])
        idx += 3
        total[sid] += score
        cnt[sid] += 1
    order = sorted(total, key=lambda s: (-total[s], s))
    out = [f"{s} {total[s]} {total[s] / cnt[s]:.2f}" for s in order]
    sys.stdout.write('\\n'.join(out) + '\\n')


solve()
''', '评分：字典一遍统计 6 分 + 多关键字排序 5 分 + 格式 :.2f 4 分'),

    ('bullets', 'T2 括号嵌套深度（15 分）★★☆☆☆', [
        '**考点**：栈、边界判断（W7）',
        '字符串含 `()[]{}` 与其他字符（|s| ≤ 10⁶）。合法则输出 `YES 最大嵌套深度`；',
        '否则输出 `NO 第一个出错位置`（1-indexed）；末尾未闭合时位置为 `len(s)+1`',
        '**样例**：`a(b[c]{d})e` → `YES 2`；`(]` → `NO 2`；`((()` → `NO 5`；`(([]))` → `YES 3`',
        '**卡点**：空串（`YES 0`）；10⁵ 层深嵌套（用递归会爆栈）；末尾未闭合',
    ]),

    ('code', 'T2 参考解答', '''import sys


def solve():
    s = sys.stdin.readline().rstrip('\\n')
    pairs = {')': '(', ']': '[', '}': '{'}
    stack, depth, best = [], 0, 0
    for i, ch in enumerate(s):
        if ch in '([{':
            stack.append(ch)
            depth += 1
            if depth > best:
                best = depth
        elif ch in pairs:
            if not stack or stack[-1] != pairs[ch]:
                print("NO", i + 1)
                return
            stack.pop()
            depth -= 1
    if stack:
        print("NO", len(s) + 1)
    else:
        print("YES", best)


solve()
''', '评分：三类错误全覆盖 8 分 + 最大深度 4 分 + len(s)+1 边界 3 分'),

    ('bullets', 'T3 会议室数量（15 分）★★★☆☆', [
        '**考点**：区间分组、差分 / 排序（W10）',
        'n 场会议（n ≤ 2×10⁵），时间是**左闭右开**区间 [aᵢ, bᵢ)，坐标 ≤ 10⁹。求最少会议室数',
        '**转化**：答案 = **同一时刻最多有多少场会议在进行**',
        '**关键细节**：左闭右开时，同一时刻的 `−1` 必须排在 `+1` 前面（先释放再占用）',
        '**卡点**：大量端点相接的区间；坐标 10⁹（不能开数组）；n=2×10⁵（不能 O(n²)）',
    ]),

    ('code', 'T3 参考解答（1/2）：事件扫描求峰值', '''import sys, heapq


def solve():
    data = sys.stdin.buffer.read().split()
    n = int(data[0])
    events, idx = [], 1
    for _ in range(n):
        a, b = int(data[idx]), int(data[idx + 1]); idx += 2
        events.append((a, 1))
        events.append((b, -1))
    events.sort()                      # (t, -1) 自动排在 (t, 1) 前面
    cur = best = 0
    for _, delta in events:
        cur += delta
        if cur > best:
            best = cur
    print(best)
''', '(t, -1) 自动排在 (t, 1) 前面 —— 左闭右开先释放再占用'),

    ('code', 'T3 参考解答（2/2）：等价的堆写法', '''def min_rooms(intervals):              # 等价的堆写法，便于理解"分组"
    intervals = sorted(intervals, key=lambda x: x[0])
    heap = []
    for a, b in intervals:
        if heap and heap[0] <= a:      # 左闭右开取 <=
            heapq.heapreplace(heap, b)
        else:
            heapq.heappush(heap, b)
    return len(heap)


print(min_rooms([(0, 30), (5, 10), (15, 20)]))     # 2
''', '便于理解"分组"的含义；左闭右开取 <='),

    ('bullets', 'T4 穿墙迷宫（15 分）★★★☆☆', [
        '**考点**：带状态的 BFS（W12）',
        'n×m 网格（≤ 500×500），`.` 空地、`#` 墙、`S` 起点、`T` 终点；',
        '**最多可穿过 k 面墙**（k ≤ 10，穿墙同样计 1 步）。求最少步数，不可达输出 `-1`',
        '**关键**：状态是 **`(x, y, 已穿墙次数)`** —— 把网格复制成 k+1 层',
        '**样例**：`S#.#T / .#.#. / .....` 中，k=0 与 k=1 均为 8，k=2 为 4',
    ]),

    ('code', 'T4 参考解答（1/2）：读入与定位', '''import sys
from collections import deque


def solve():
    data = sys.stdin.read().split()
    n, m, k = int(data[0]), int(data[1]), int(data[2])
    grid = data[3:3 + n]
    sx = sy = tx = ty = -1
    for i in range(n):
        for j in range(m):
            if grid[i][j] == 'S': sx, sy = i, j
            elif grid[i][j] == 'T': tx, ty = i, j
''', '状态是 (x, y, 已穿墙次数) —— 把网格复制成 k+1 层'),

    ('code', 'T4 参考解答（2/2）：三维 BFS 主循环', '''    DIRS = ((-1, 0), (1, 0), (0, -1), (0, 1))
    dist = [[[-1] * m for _ in range(n)] for _ in range(k + 1)]
    dist[0][sx][sy] = 0
    q = deque([(sx, sy, 0)])
    while q:
        x, y, u = q.popleft()
        if (x, y) == (tx, ty):
            print(dist[u][x][y]); return
        for dx, dy in DIRS:
            nx, ny = x + dx, y + dy
            if not (0 <= nx < n and 0 <= ny < m):
                continue
            nu = u + (1 if grid[nx][ny] == '#' else 0)
            if nu > k or dist[nu][nx][ny] >= 0:
                continue
            dist[nu][nx][ny] = dist[u][x][y] + 1
            q.append((nx, ny, nu))
    print(-1)


solve()
''', '每步代价都是 1，所以 BFS 就够；若穿墙代价不是 1，必须换 Dijkstra'),

    ('bullets', 'T5 作业时间分配（20 分）★★★★☆', [
        '**考点**：0-1 背包 + 次要目标（W11）',
        'n ≤ 100 份作业（耗时 tᵢ、得分 sᵢ），总时间 T ≤ 10⁴，每份要么完整完成要么不做',
        '求**最高总分**；若多种方案同分，再求其中**总耗时最少**的那个耗时',
        '**双目标 DP**：`dp[c]` = 最高分，`tm[c]` = 达到该分的最小耗时',
        '**样例**：T=10，(3,10)(4,14)(5,16)(2,5) → `31 10`',
    ]),

    ('code', 'T5 参考解答', '''import sys


def solve():
    data = sys.stdin.read().split()
    n, T = int(data[0]), int(data[1])
    dp = [0] * (T + 1)          # 最高得分
    tm = [0] * (T + 1)          # 达到 dp[c] 的最小耗时
    idx = 2
    for _ in range(n):
        t, s = int(data[idx]), int(data[idx + 1]); idx += 2
        for c in range(T, t - 1, -1):        # 0-1 背包：倒序
            cand = dp[c - t] + s
            if cand > dp[c] or (cand == dp[c] and tm[c - t] + t < tm[c]):
                dp[c] = cand
                tm[c] = tm[c - t] + t
    best = max(dp)
    print(best, min(tm[c] for c in range(T + 1) if dp[c] == best))


solve()
''', '⚠️ 两个易错点：必须倒序；次要目标条件必须是 cand == dp[c]，写成 >= 会答错'),

    ('bullets', 'T6 电网巡检（20 分）★★★★★', [
        '**考点**：最小瓶颈路 = 排序 + 并查集（W6、W9）；亦可二分 + BFS（W12）',
        'n ≤ 10⁵ 个变电站、m ≤ 2×10⁵ 条带危险度的双向线路。',
        '路径的"风险"= 路径上危险度的**最大值**；求 1 → n 所有路径中风险的**最小值**',
        '**核心洞察**：**按危险度从小到大逐条加边，第一次让 1 与 n 连通的那条边就是答案**',
        '**卡点**：不连通输出 `-1`；重边；链状图（未做路径压缩会 TLE）',
    ]),

    ('code', 'T6 参考解答（1/2）：读入并按危险度升序', '''import sys


def solve():
    data = sys.stdin.buffer.read().split()
    n, m = int(data[0]), int(data[1])
    edges, idx = [], 2
    for _ in range(m):
        u, v, w = int(data[idx]), int(data[idx+1]), int(data[idx+2]); idx += 3
        edges.append((w, u, v))
    edges.sort()                              # 按危险度升序
''', '排序是整个算法的关键一步'),

    ('code', 'T6 参考解答（2/2）：并查集 + 首次连通即答案', '''    parent = list(range(n + 1))
    size = [1] * (n + 1)

    def find(x):
        root = x
        while parent[root] != root:
            root = parent[root]
        while parent[x] != root:              # 路径压缩（迭代，防爆栈）
            parent[x], x = root, parent[x]
        return root

    for w, u, v in edges:
        ru, rv = find(u), find(v)
        if ru != rv:
            if size[ru] < size[rv]:
                ru, rv = rv, ru
            parent[rv] = ru
            size[ru] += size[rv]
        if find(1) == find(n):
            print(w); return
    print(-1)


solve()
''', '路径压缩写成迭代两趟，防止 n=10^5 时爆栈'),

    ('section', '第 5 节', '给学生的备考建议'),

    ('table', '必须能默写的 12 个模板', [
        ['#', '模板', '周次'],
        ['1–3', '快速输入 ｜ 埃氏筛 ｜ 前缀和 + 差分', 'W4、W6、W10'],
        ['4–5', '多关键字排序 ｜ 归并的合并（逆序对）', 'W6'],
        ['6–7', '单调栈 ｜ 回溯模板', 'W7、W9'],
        ['8', '并查集（路径压缩 + 按大小合并）', 'W9'],
        ['9–10', '0-1 / 完全背包 ｜ LIS 的 O(n log n)', 'W11'],
        ['11–12', 'BFS（deque + 入队标记 + 状态维）｜ 二分答案', 'W12'],
    ], '自测方法：关掉所有资料，在空文件里默写'),

    ('two', '一页 A4 cheat sheet 的建议内容',
     '正面', ['快速输入 / 输出模板',
              '复杂度速查表（n 范围 -> 复杂度）',
              '容器操作复杂度表',
              '多关键字排序、bisect 用法',
              '前缀和 / 差分公式',
              '二分模板（两种取整方向）'],
     '反面', ['回溯模板骨架',
              '并查集（10 行）',
              '背包两行核心循环 + inf 初始化',
              'BFS 模板（含状态维）',
              'Dijkstra 模板（heapq）',
              '高频陷阱清单']),

    ('ascii', '考场流程', r"""
   0-5 min      通读 6 题，按预估难度标序，先做有把握的
   5-15 min     T1（签到）—— 写完就交，用 OJ 反馈代替自己检查
   15-45 min    T2、T3
   45-78 min    T4、T5
   78-102 min   T6 或回头补前面卡住的题
   102-112 min  检查输出格式：多余空格、换行、精度、特殊边界
""", '六条纪律：看范围、早提交、造数据、查复杂度、按时换题、留时间查格式'),

    ('bullets', '高频陷阱清单（考前最后一遍）', [
        '☐ 忘 `int()` / 忘 `strip()` ｜ ☐ `[[0]*n]*m` 别名陷阱',
        '☐ `x in list` 是 O(n)；`list.pop(0)` 是 O(n)',
        '☐ 浮点用 `==` 比较；`int(x**0.5)` 差 1',
        '☐ 回溯忘 `path[:]` 拷贝 / 忘还原状态 ｜ ☐ 0-1 背包写成正序',
        '☐ "恰好装满"没用 `±inf` 初始化 ｜ ☐ BFS 出队时才标记 visited',
        '☐ 带状态的搜索少加了一维 ｜ ☐ 二分答案的取整方向写反',
        '☐ 多关键字排序只写了一个 key ｜ ☐ 递归过深未处理',
        '☐ 输出格式：多余空格 / 换行 / 精度（`85.00` 而非 `85.0`）',
    ]),

    ('key', '三条硬性提醒',
     '考试禁止任何 AI 工具；讲不清自己的代码按学术不端处理；自检方法是关掉窗口从空文件重写。'),

    ('section', '第 6 节', '命题与阅卷检查清单（教师用）'),

    ('bullets', '命题前 / 参考解答', [
        '☐ 6 题覆盖至少 5 个大类，贪心 / DP / 搜索各至少 1 题',
        '☐ 难度梯度符合 ★1 → ★5，T1 是真正的签到题',
        '☐ 每题数据范围明确，与预期复杂度匹配；易误解的输出定义给出**样例解释**',
        '☐ 参考解答**已实际运行**并通过全部样例',
        '☐ 已测边界：n=1、空输入、全相同、极值、不连通',
        '☐ 已与暴力解**对拍**（本卷 T3/T4/T5/T6 各做了 400 组随机对拍）',
        '☐ 递归解法已确认不会在评测机上爆栈（或已改迭代）',
    ]),

    ('bullets', '测试数据 / 阅卷', [
        '☐ 每题都有**卡错解**的数据，且已用错误实现验证过它真能卡住',
        '☐ 每题都有**卡超时**的数据；分档数据设计合理',
        '☐ `tools/redteam_exam.py` 已跑过 T1–T6 的 WA / TLE / MLE 反例',
        '☐ 时限按参考解答实测时间的 **3–5 倍**设定，并考虑 Python 与 C++ 的差异',
        '☐ 评分标准逐项可核对',
        '☐ 抽查高分卷，要求学生**口头解释自己的代码**',
        '☐ 记录各题实际 AC 率，与预期对照，用于下一年校准',
    ]),

    ('bullets', '小结', [
        '知识体系四条线：**计算机基础、语言与工具、算法与数据结构、AI 素养**',
        '机考 **6 题 / 112 分钟**，**禁用一切 AI**，**讲不清代码按学术不端处理**',
        '样卷六题：**字典排序、栈、区间分组、带状态 BFS、双目标背包、最小瓶颈路**',
        '备考只做两件事：**默写 12 个模板** + **手写一页 cheat sheet**',
        '考场六条：看范围、早提交、造数据、查复杂度、按时换题、留时间查格式',
    ]),

    ('key', '课程到此结束',
     '你完成的 150–200 道题、调过的每一个 WA、重写过的每一份代码，才是这门课真正留下的东西。'),
]
