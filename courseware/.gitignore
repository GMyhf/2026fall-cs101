__pycache__/
*.pyc
.videowork/
# 1080p 成品不入库：一周 18–32 MB、16 周合计约 330 MB，每次重建整个文件都变。
# 讲稿、字幕、时间轴都在库里，`python3 make_video.py NN` 可以随时重建。
*.mp4
# 但 720p 轻量版入库：换台机器就能直接看，不用先装 edge-tts / ffmpeg 重建一遍。
# （16 周合计约 183 MB。嫌大就删掉下面这行豁免，改用 Git LFS 或课程网盘 —— 源都在库里。）
!video/*-preview.mp4
